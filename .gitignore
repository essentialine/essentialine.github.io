_site/
.jekyll-cache/
*.xmp
Gemfile.lock
_drafts/
_process/
_blocked-holding/

# Syncthing-local Claude context — never publish
CLAUDE.md
INFLUENCES.md

# gThumb metadata directories — never publish
**/.comments/

# local symlinks to scan working dirs — never publish
*.blick.tracing

# diptych.py build intermediates
.diptych-work/
